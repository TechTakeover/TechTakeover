pwd
cd (change directories /absolute or relative/)
history | sort | less
history | less
man (Manual of page) (able to see all available commands)

la (list all)
git init (creates git repository)
ls (list)

ls -o (list removing group column)

can combine flags together

Unix is most stable
Can perform multiple processes at once

