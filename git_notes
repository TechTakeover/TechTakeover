Repository

man git-config (get manual for only git config)

git status (looks inside staging area)

git add (adds files to staging area)

git add. (adds everything from current directory)

makes a commit| ID, Message, Timestamp, Differences, Signature

git commit 'Message' (commits files with message)

Red X (something untracked or in staging area)
Green Check (nothing in staging area or untracked files)

use present tense when creating message

git la (list all)

initialize repository once
add files
commit
modify
add
commit
//Labels//
Head points to most recent commit
Master also points to most recent commit
//Label//

